(library
 ((name eqaf_xen)
  (public_name eqaf.xen)
  (optional)
  (libraries (mirage-xen))
  (c_names (safeq))
  (c_flags (:include cflags.sexp))))

(rule (copy# ../safeq.c safeq.c))

(rule
 ((targets (cflags.sexp))
  (deps (cflags.sh))
  (action (bash "./${<} > ${@}"))))
